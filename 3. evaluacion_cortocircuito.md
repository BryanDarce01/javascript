# Evaluación de Cortocircuito
Lo  que hace este codigo es evaluar si la condición en Truthy y si es Truthy, ejecuta el código siguiente.

Es como un operador ternario pero sin la segunda condición y sin el signo ?
```javascript
const auth = true
auth && console.log('Usuario autenticado')
```